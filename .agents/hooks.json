{
  "enabled": true,
  "PreToolUse": [],
  "PostToolUse": [],
  "PreInvocation": [
    {
      "command": "node .agents/scripts/load-working-memory.js",
      "description": "Auto-load working memory context — hiển thị trạng thái hiện tại từ NOTES.md đầu mỗi invocation."
    },
    {
      "command": "node .agents/scripts/track-session-metrics.js --show",
      "description": "Hiển thị dashboard metrics (tokens, turns, cảnh báo) đầu mỗi invocation để theo dõi chi phí và context window."
    }
  ],
  "PostInvocation": [
    {
      "command": "node .agents/scripts/calculate-current-turn-tokens.js --update",
      "description": "Tự động tính toán và cập nhật số lượng input/output tokens và turns sau khi agent phản hồi."
    }
  ],
  "Stop": [
    {
      "command": "node .agents/scripts/track-session-metrics.js --end",
      "description": "Lưu tổng kết metrics khi agent kết thúc phiên làm việc (Stop event)."
    }
  ]
}
